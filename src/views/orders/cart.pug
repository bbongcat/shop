extends ../base
include ../mixins/cartTemplate

block content
    h3 #{loggedInUser.name}님의 장바구니
    hr
    each item in products
         +cart(item)
    else
        p 장바구니가 비어있습니다.


    form(method="POST")
        input(type="submit", value="선택 상품만 주문", formaction="")
        input(type="submit", value="전체 상품 주문", formaction="")
        input(type="submit", value="선택 상품 삭제", formaction="")
        input(type="submit", value="장바구니 비우기", formaction="")